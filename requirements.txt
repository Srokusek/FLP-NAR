# Python 3.12 (matches notebooks/showcase.ipynb)
ipykernel==7.2.0
matplotlib==3.10.8
numpy==2.4.2
pandas==3.0.1
pyomo==6.9.5
torch==2.10.0
torch-geometric==2.7.0
tqdm==4.67.3

# The notebook's exact=True path also requires the GLPK `glpsol` executable.
# Install it with your system package manager (for example: apt install glpk-utils).
